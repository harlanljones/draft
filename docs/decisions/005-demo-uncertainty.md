# D-005: Demo Uncertainty Semantics

Status: accepted for phase 1 only.

The interval is a residual-scale demonstration widened for sparse observations and multiplied by
1.65 for high-school fixtures. `probability_two_war` is a logistic transformation around the
invented two-unit target. Neither quantity is empirically calibrated. Empirical semantics remain
open until the target, horizon, cohort, and data coverage are approved.
